FROM node:20-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
CMD ["sh", "-c", "npx vitest run tests/unit tests/api --reporter=verbose && npx vitest run --config vitest.integration.config.js --reporter=verbose"]
